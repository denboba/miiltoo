# NOTE: A local `.env` is not required for Firebase configuration in this project.
# This project uses the generated `lib/firebase_options.dart` (from FlutterFire)
# which contains the Firebase API keys and project settings for each platform.
# Keep this template for reference or if you want to store additional local
# secrets (e.g. optional development-only keys). By default you can ignore
# creating a `.env` file.

# Optional reference keys:

